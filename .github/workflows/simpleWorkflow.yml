name: workflow overview


on:
  workflow_dispatch:
  push:
    branches:
      - main


jobs:
  selfhostedjob:
    name: selfhosted job 
    runs-on: self-hosted
    steps:
      - name: self hosted step
        id: selfhostedid1
        shell: cmd
        run: |
          echo this is a multiline comment
          for the sef hosted runner
  simplejob1:
    name: firstjob
    runs-on : ubuntu-latest
    steps:
      - name: name of my first step
        id: step1
        run: echo "this is my first step"
      - name : name of my second step
        id: step2
        shell: python
        run : print("this is my second step")
       
      
  
